---
spring:
  data:
    mongodb:
      host=localhost
      port=27017
      database=demo
      username=mhoikka
      password=mhoikka
---
server:
  port: 443
  ssl:
    enabled: true
    key-store-type: PKCS12
    key-store: classpath:keystore/keystore.p12
    key-store-password: ${KEYSTORE_PASSWORD} #TODO Put the real password in a env file
    protocol: TLS
    enabled-protocols: TLSv1.2
---

